interface:
  display_name: "Swift Functional Architecture"
  short_description: "Swift 6.2+ layers, stores, and executors"
  default_prompt: "Use $swift-functional-architecture to design this Swift feature with pure reducers, thin orchestration shells, named effect executors, and factory-based wiring."

policy:
  allow_implicit_invocation: true
